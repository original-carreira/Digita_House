package Functions_Aula_15

class Caixa {
    inline fun fechamentoDeConta(
        cabecalhoDoComprovante: Unit,
        dataCompra: Unit,
        listaCarrinho: ArrayList<Produto>,
        imprimeListaCompra: Unit,
        adicionaRodape: Unit
    ){
        /*cabecalhoDoComprovante.invoke()
        println(dataCompra.invoke())
        imprimeListaCompra.invoke(listaCarrinho)
        println("${adicionaRodape.invoke(listaCarrinho)}")
        */

    }
}